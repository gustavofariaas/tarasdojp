#include<stdio.h>
#include<stdlib.h>

typedef struct{
    char hero[50];
    int poder;
} heroes

//simulaçao, a funçao abaixo ira imprimir o gabarito da batalha
void simularBatalha(heroes vetor[], heroes *thanos){

}

//gameplay,o mais importante
void imprime(heroes *herois[], heroes *thanos){



heroes**selecaoParaAtaque(heroes veto[])
}
void leHeroi(herois * t){
    int i=0
    FILE * entrada;
    entrada = fopen("herois.txt", "r");
    if(entrada!=NULL){
        fscanf(entrada,"%s",thanos->heroi);
        fscanf(entrada,"%d",thanos->poder);
        do{
            fscanf(entrada,"%s",herois[i]->heroi);
            fscanf(entrada,"%d",herois[i]->poder)

        }while(!feof(entrada));

        }else{}

    }


}



int main(void){
heroes thanos;
leHeroi(&thanos-> heroi);
leHeroi(&thanos-> poder);

heroes herois[11];
for (i=0; i < 11; i++){
    leHeroi(&herois[i]->heroi);
    leHeroi(&herois[i]->poder);
    }
return 0;
}
